import React from "react";
import ReactDOM from "react-dom/client";
import QuotationBuilder from "../quotation-builder.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <QuotationBuilder />
  </React.StrictMode>
);
